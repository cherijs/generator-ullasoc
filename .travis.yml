language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ullalaa' ]; then cd .. && eval "mv $currentfolder generator-ullalaa" && cd generator-ullalaa; fi

